<main>

        <section class="ticket-section section-padding gallery-section">
            
        
            <div class="container">
                {% for picture in pictures %}
                    {% if forloop.counter0|divisibleby:2 %}
                        <div class="row mt-4">
                    {% endif %}
        
                    <div class="col-lg-6 col-10 mx-auto d-flex justify-content-center mb-4">
                        <a class="navbar-brand text-center" href="{{ picture.get_url }}">
                            <div class="image__m gallery-image-container">
                                <img class="logo img-fluid image__img gallery-image" src="{{ picture.images.url }}" alt="{{ picture.title }}">
                                <div class="image__overlay gallery-overlay">
                                    <div class="image__title gallery-title">Click</div>
                                    <p class="image__description gallery-description">for more of this project</p>
                                </div>
                            </div>
                            <!-- <h1 class="text-white">Click for more</h1> -->
                        </a>
                    </div>
        
                    {% if forloop.counter0|divisibleby:2 or forloop.last %}
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        </section>
        
        
        
        
    </main>

U Django web frameworku, aplikacije su samostalne komponente ili 
moduli koji služe za organizaciju i strukturiranje različitih dijelova 
funkcionalnosti unutar web aplikacije.
Aplikacije u Django-u su neovisne jedna o drugoj, što znači da svaka 
aplikacija može imati svoje modele podataka, logiku ,predloške i URL rute.